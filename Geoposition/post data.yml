info:
  name: post data
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/test
  body:
    type: json
    data: |-
      {
        "name": "Tom",
        "email": "tom@mail.ru",
        "message": "Tom is exist"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
