info:
  name: get data Auth
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/test
  auth:
    type: bearer
    token: valid-token

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
